import { ContentExistPipe } from './content-exist.pipe';

describe('ContentExistPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentExistPipe();
    expect(pipe).toBeTruthy();
  });
});
